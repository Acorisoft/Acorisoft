<mui:DialogView x:Class="Acorisoft.Morisa.Tools.Views.NewBrushSetDialogStep3View"
                x:TypeArguments="vm:NewBrushSetDialogStep3ViewModel"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                xmlns:vm="clr-namespace:Acorisoft.Morisa.Tools.ViewModels"
                xmlns:rx="http://reactiveui.net"
                xmlns:mui="https://github.com/Acorisoft/Morisa/UI"
                mc:Ignorable="d" 
                d:DesignHeight="600" d:DesignWidth="800"
                Width="800"
                Height="600"
                d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True,Type={x:Type vm:NewBrushSetDialogStep3ViewModel}}">

    <mui:DialogContentPresenter Title="新建画刷集" 
                                Subtitle="选择一个画刷的保存目录。">
        <mui:FormGroup Header="保存目录" 
                       Margin="64 0">
            <mui:FormGroup.Control>
                <TextBox FontSize="20" Text="{Binding File,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
            </mui:FormGroup.Control>

            <Button Content="..."
                    Width="64"
                    Height="32"
                    Click="SelectFolder"/>
        </mui:FormGroup>
        <mui:DialogContentPresenter.ButtonGroup>
            <UniformGrid Columns="3" Height="48" DockPanel.Dock="Bottom">
                <Button Command="{x:Static mui:DialogCommands.LastStep}" Content="上一步" BorderThickness="0"/>
                <Button Command="{x:Static mui:DialogCommands.Skip}" Content="跳过" BorderThickness="0"/>
                <Button Command="{x:Static mui:DialogCommands.Completion}" Content="完成" BorderThickness="0"/>
            </UniformGrid>
        </mui:DialogContentPresenter.ButtonGroup>
    </mui:DialogContentPresenter>
</mui:DialogView>