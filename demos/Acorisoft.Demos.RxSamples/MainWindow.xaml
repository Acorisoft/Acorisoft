<Window x:Class="Acorisoft.Demos.RxSamples.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Acorisoft.Demos.RxSamples"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="48" />
        </Grid.RowDefinitions>

        <ItemsControl DisplayMemberPath="Text" ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=local:MainWindow},Path=Collection,Mode=OneWay}"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="《" Click="Last"/>
            <Button Content="》" Click="Next"/>
            <Button Content="Search" Click="Search"/>
        </StackPanel>
    </Grid>
</Window>
